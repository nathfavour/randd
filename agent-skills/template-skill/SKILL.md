---
name: template-skill
description: |
  A template skill following the Agent Skills Open Standard.
  Triggers: "Use this as a starting point for creating new agent skills."
---

# Instructions
1. Use the `scripts/` directory for any logic that isn't purely natural language.
2. Store large reference files in `references/`.
3. Provide example inputs/outputs in `assets/`.
4. Add verification steps in `tests/`.
